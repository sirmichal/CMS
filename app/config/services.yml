# Learn more about services, parameters and containers at
# http://symfony.com/doc/current/book/service_container.html
parameters:
#    parameter_name: value

services:
    footer_handler:
        class: AdminBundle\Service\FooterService
        arguments: ['@form.factory', '@doctrine', '@session']

    media_listener:
        class: AdminBundle\Service\MediaListener
        tags:
            - { name: kernel.event_subscriber }