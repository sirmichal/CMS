{# Written by Michał Turemka <michal.turemka@gmail.com> #}

<div class="modal-dialog">
    <div class="modal-content">
        <div class="modal-body">
            <div class="row">
                <div class="col-md-10">
                    <img class="img-responsive" id="single-img" ng-src="{*{ ctrl.img.cacheImagePath }*}">
                </div>
                <div class="col-md-2">
                    <p><b>Wielkość:</b> {*{ ctrl.img.width }*} x {*{ ctrl.img.height }*}</p>
                    <p><b>Rozmiar:</b> {*{ ctrl.img.size }*}b</p>
                    <p><b>Typ:</b> {*{ ctrl.img.mime }*}</p>
                    <a ng-click="ctrl.deleteImg(ctrl.img.id)">Usuń</a>
                </div>
            </div>
        </div>
    </div>
</div>
