{% extends 'AdminBundle::base.html.twig' %}
{% block content %}

    <table class="table table-striped table-condensed">
        <thead>
        <tr>
            <th>Tytuł</th>
            <th>Treść</th>
            <th>Użytkownik</th>
            <th>Dodano</th>
        </tr>
        </thead>
        <tbody>
        {% for post in posts %}
            <tr>
                <td>{{ post.title }}</td>
                <td>{{ post.content }}</td>
                <td>{{ post.user }}</td>
                <td>{{ post.created|date("d-M-Y H:i") }}</td>
                <td><a href={{ path('edit_post', {'postId': post.id}) }}>Edytuj</a></td>
            </tr>
        {% endfor %}

        </tbody>
    </table>

{% endblock %}