{# Written by Michał Turemka <michal.turemka@gmail.com> #}

<nav class="navbar-fixed-top text-center cl-effect-1">
    <div class="container">
        <div class="row" id="nav-title-wrapper" >
            <div class="col-md-12 ">
                <a class="site-title" href={{ path("homepage") }}>{{ literals.title }}</a>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <ul id="menu">
                    {% for c in categories %}
                        <li class="navigation-link text-uppercase">
                            <a href={{ path("category", { "id": c.id }) }}>{{ c.name }}</a>
                        </li>
                    {% endfor %}
                </ul>
            </div>
        </div>
    </div>
</nav>
